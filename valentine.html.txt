<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Be My Valentine, Tierra?</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      font-family: 'Comic Sans MS', cursive;
      display: flex;
      align-items: center;
      justify-content: center;
      background: radial-gradient(circle, #ff1e56, #8b0000);
      overflow: hidden;
    }

    .card {
      background: #fff0f3;
      padding: 35px;
      border-radius: 25px;
      text-align: center;
      box-shadow: 0 15px 40px rgba(0,0,0,0.3);
      max-width: 380px;
      z-index: 2;
      position: relative;
    }

    h1 {
      color: #c9184a;
    }

    p {
      color: #6a040f;
      font-size: 18px;
    }

    .photos {
      display: flex;
      gap: 10px;
      justify-content: center;
      margin: 15px 0;
    }

    .photos img {
      width: 90px;
      height: 90px;
      border-radius: 15px;
      object-fit: cover;
      border: 3px solid #ff4d6d;
    }

    button {
      padding: 14px 30px;
      font-size: 18px;
      border-radius: 40px;
      border: none;
      cursor: pointer;
      margin: 10px;
      font-weight: bold;
    }

    #yesBtn {
      background: linear-gradient(135deg, #ff4d6d, #c9184a);
      color: white;
      box-shadow: 0 5px 15px rgba(255,77,109,0.6);
    }

    #noBtn {
      background-color: #ffccd5;
      color: #6a040f;
      position: absolute;
    }

    /* SHAKE ANIMATION */
    @keyframes shake {
      0% { transform: translateX(0); }
      25% { transform: translateX(-6px); }
      50% { transform: translateX(6px); }
      75% { transform: translateX(-6px); }
      100% { transform: translateX(0); }
    }

    .shake {
      animation: shake 0.3s;
    }

    .hearts span {
      position: absolute;
      font-size: 26px;
      animation: float 5s linear infinite;
      opacity: 0.8;
    }

    @keyframes float {
      0% { transform: translateY(100vh) scale(1); }
      100% { transform: translateY(-10vh) scale(1.6); }
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>❤️ Tierra ❤️</h1>
    <p>Swirl, will you be my Valentine?</p>

    <div class="photos">
      <img src="photo1.jpg">
      <img src="photo2.jpg">
      <img src="photo3.jpg">
    </div>

    <p>You’re my favorite person forever 💘</p>

    <button id="yesBtn" onclick="yesClicked()">YES BABY 💕</button>
    <button id="noBtn" onclick="noClicked()">No 😐</button>
  </div>

  <div class="hearts"></div>

  <script>
    let noClicks = 0;

    function yesClicked() {
      document.body.innerHTML = `
        <div class="card">
          <h1>💖 SHE SAID YES 💖</h1>
          <p>Happy Valentine’s Day, Swirl ❤️</p>
          <p>I choose you always.</p>
        </div>
      `;
    }

    function noClicked() {
      const btn = document.getElementById("noBtn");
      noClicks++;
      btn.classList.add("shake");

      setTimeout(() => btn.classList.remove("shake"), 300);

      if (noClicks >= 2) {
        btn.style.display = "none";
      }
    }

    const hearts = document.querySelector('.hearts');
    for (let i = 0; i < 30; i++) {
      const heart = document.createElement('span');
      heart.innerHTML = '❤️';
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDelay = Math.random() * 4 + 's';
      hearts.appendChild(heart);
    }
  </script>

</body>
</html>